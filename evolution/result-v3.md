# Result V3 - Content-Addressed Distribution Evidence

A01 adds deterministic artifact descriptors and lifecycle assessments with
explicit media type, size, SHA-256 identity, active/stale/revoked/superseded
status, assessment/expiry times, optional successor identity, supporting
descriptors, validation, canonical JSON, and digest helpers. The records carry
caller-supplied evidence only; they do not implement a registry or grant trust.
